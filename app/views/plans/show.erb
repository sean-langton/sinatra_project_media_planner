<div class="content">
<h2> <%= @plan.plan_name %> <a href="<%=@plan.id %>/edit"> Edit </a></h2>
<h3> $<%= @plan.plan_budget.to_s.reverse.scan(/\d{3}|.+/).join(",").reverse %> </h3>
<h3> Remaining Budget: $<%= @remaining_budget.to_s.reverse.scan(/\d{3}|.+/).join(",").reverse %> </h3>
<table>
  <tr>
    <th> Channel Name </th>
    <th> Channel Budget </th>
  </tr>
  <% @plan.channels.each do |c| %>
  <tr>
  <td> <%= c.channel_name %> </td>
  <td> $<%= c.channel_budget.to_s.reverse.scan(/\d{3}|.+/).join(",").reverse %> </td>
  </tr>
  <% end %>
</table>
<br>
<form method="post" action="/plans/<%= @plan.id %>">
  <input id="hidden" type="hidden" name="_method" value="delete">
  <input type="submit" value="Delete Plan">
</form>
</div>
